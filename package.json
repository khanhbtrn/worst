{
  "name": "probably-fine",
  "version": "2.0.0",
  "description": "The worst UX app on purpose",
  "private": true,
  "scripts": {
    "dev": "python3 -m http.server 8080",
    "deploy": "vercel deploy --prod --yes"
  }
}
